# macOS
.DS_Store

# IDE
.idea/
*.iml
.vscode/

# Android
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/app/release/
android/captures/
*.apk
*.aab
*.keystore

# Web
web/node_modules/
web/dist/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
ml/artifacts/
ml/runs/

# Collected corpora stay local; only the small English seed is tracked
ml/data/corpus/

# Models (training outputs stay out of git; the shipped baseline asset is the
# one exception — it's ~130 KB and must stay in lockstep with the app code.
# The future transformer model is too big for git: LFS or release artifact.)
*.tflite
*.onnx
*.pt
*.h5
!android/app/src/main/assets/trigger_classifier.tflite

# Misc
*.log
.env
private/*
