org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Integration endpoints/keys, empty in the open repo. Override per-machine in
# ~/.gradle/gradle.properties or via -P flags; never commit real values.
samind.chatApiBaseUrl=
samind.chatApiKey=
samind.modelUpdateUrl=
samind.sentryDsn=
